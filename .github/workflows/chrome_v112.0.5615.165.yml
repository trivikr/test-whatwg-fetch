name: chrome_v112.0.5615.165

on:
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/chrome.yml
    with:
      # Closest available commit position to official v100 from From https://chromiumdash.appspot.com/releases?platform=Linux
      # Verified from logs that this is Chromium 100.0.4896.0
      chrome-version: "972765"
